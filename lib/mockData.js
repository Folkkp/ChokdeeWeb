import realData from './realData.json';

// We replaced the generated mock data with real historical data scraped from Sanook.
// We keep the variable name mockLotteryData so the rest of the application still works without changes.
export const mockLotteryData = realData;
